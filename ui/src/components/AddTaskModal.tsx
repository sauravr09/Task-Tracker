// Open Modal when clicked on add task: 

// A task will be something like: maybe we want the Id to be number and date be Date'
type Task = {
  id: string
  title: string
  description: string
  tags: { label: string; variant: string }[]
  ref: string
  date: string
}
